/**
 * Barometer / rough temperature estimate
 * Phone barometer gives rough ambient estimate, NOT accurate room temp
 * Supplement with user-input thermostat setting
 */

// Placeholder - implementation in Step 6
export class BarometerSensor {
  async start(): Promise<void> {}
  async stop(): Promise<void> {}
  getEstimatedTempF(): number | null { return null; }
}
